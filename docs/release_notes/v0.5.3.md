# Release v0.5.3

## What's New
* **Demo Ready Coverage:** Added comprehensive tests for `yt` (YouTube) and `ratsay` (mascot) commands to ensure a smooth "Live Demo" experience.
* **Modular Docs:** Updated `GEMINI.md` and `docs/development.md` to reflect the new modular command handler structure.

## Bug Fixes
* **Command Chaining Fix:** Resolved a crash where semicolon-chained commands (e.g., `home; launch Netflix`) would fail when run through the `RokuService` layer due to missing UI context.
* **Dependency Stability:** Restored missing development dependencies (`rich`, `pytest-cov`, `respx`) to `pyproject.toml`.

## Under the Hood
* **SemVer Bump:** Promoted version to `v0.5.3`.
* **Verification:** All 105 tests are passing cleanly with full linting and type safety compliance.
